# Normalize line endings to LF in the repo; checkout as-is on Windows
* text=auto eol=lf

# Jupyter notebooks
*.ipynb text eol=lf

# Data files — treat as binary to avoid line-ending mangling
*.data binary
*.mod  binary
